def max_number(x, y):

    if x > y:
        print "%d is greater than %d" % (x, y)
    elif x < y:
        print "%d is greater than %d" % (y, x)
    else:
        print "%d is equal to %d" % (x, y)


max_number(30, 50)
